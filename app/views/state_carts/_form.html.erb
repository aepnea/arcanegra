<%= form_for(@state_cart) do |f| %>
  <% if @state_cart.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@state_cart.errors.count, "error") %> prohibited this state_cart from being saved:</h2>

      <ul>
      <% @state_cart.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :state %><br>
    <%= f.text_field :state %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
