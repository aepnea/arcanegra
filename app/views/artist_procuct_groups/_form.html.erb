<%= form_for(@artist_procuct_group) do |f| %>
  <% if @artist_procuct_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@artist_procuct_group.errors.count, "error") %> prohibited this artist_procuct_group from being saved:</h2>

      <ul>
      <% @artist_procuct_group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_group_id %><br>
    <%= f.text_field :product_group_id %>
  </div>
  <div class="field">
    <%= f.label :article_id %><br>
    <%= f.text_field :article_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
