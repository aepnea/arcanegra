<%= form_for(@product_product_attribute) do |f| %>
  <% if @product_product_attribute.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_product_attribute.errors.count, "error") %> prohibited this product_product_attribute from being saved:</h2>

      <ul>
      <% @product_product_attribute.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :product_id %><br>
    <!--<%= f.number_field :product_id %> -->
    <%= f.collection_select :product_id, @product, :id, :name %>

  </div>
  <div class="field">
    <%= f.label :product_attribute_id %><br>
    <!--<%= f.number_field :product_attribute_id %>-->
    <%= f.collection_select :product_attribute_id, @product_attribute, :id, :name %>

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
