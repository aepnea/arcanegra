<%= form_for(@payment_status) do |f| %>
  <% if @payment_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@payment_status.errors.count, "error") %> prohibited this payment_status from being saved:</h2>

      <ul>
      <% @payment_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status %><br>
    <%= f.text_field :status %>
  </div>
  <div class="field">
    <%= f.label :payment_type_id %><br>
    <%= f.text_field :payment_type_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
